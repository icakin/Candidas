# OS / editor
.DS_Store
*~
.Rproj.user/
.Rhistory
.RData
.RDataTmp*

# Python
__pycache__/
*.pyc
.venv/
venv/

# stray R plot dumps
Rplots.pdf

# scratch
_to_delete/

# large / external / regenerable — uncomment to keep out of git:
# etcGEMs/            # external package (git clone GabYvonDurocher/etcGEMs)
# figures/            # regenerated by scripts/2*_*.R + schematic.py
# models/             # regenerated by 10_bayesian_models.R

# --- external sub-projects (their own git repos / GitHub) ---
/etcGEMs/

# --- large regenerable data blobs (kept out of git; regenerate from data/) ---
Oxygen_All_Long.csv
Oxygen_Data_Smoothed_Trimmed.csv
Oxygen_Data_Filtered.csv
per_series_fits.pdf
/genomes/

# --- Quarto / build artefacts ---
/.quarto/
**/*.quarto_ipynb
_output/
submission/

# --- C1: environment + non-destructive reproduction outputs ---
/renv/library/
/renv/local/
/renv/cellar/
/renv/lock/
/renv/python/
/renv/sandbox/
/renv/staging/
cauris_etcgem/.venv/
/logs/

# --- C1 reproduction run: keep the tables and the manuscript figures, not the
#     regenerable bulk (68 MB of diagnostic figures, 29 MB of brms fits, and the
#     22 MB staging render). Everything here is reproduced by run_c1.sh.
/results_C1/figures/*
!/results_C1/figures/manuscript/
/results_C1/rds/
/results_C1/manuscript_build/
